{
  "_comment": "Deliberately empty. Every deployed value comes from an environment variable via custom-environment-variables.json; this file exists so node-config's strict mode does not warn that NODE_ENV=production has no matching config file."
}
