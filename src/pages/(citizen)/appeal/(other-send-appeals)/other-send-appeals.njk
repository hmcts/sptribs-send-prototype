{% extends "_layouts/citizen.njk" %}
{% from "govuk/components/back-link/macro.njk" import govukBackLink %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/error-summary/macro.njk" import govukErrorSummary %}
{% from "govuk/components/radios/macro.njk" import govukRadios %}

{% block beforeContent %}
  {{ govukBackLink({ text: back, href: backHref }) }}
{% endblock %}

{% block content %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      {% if errors | length %}
        {{ govukErrorSummary({ titleText: errorSummaryTitle, errorList: errors | govukErrorSummary }) }}
      {% endif %}

      <form method="post" novalidate>
        {% include "_partials/csrf-input.njk" %}
        <span class="govuk-caption-l">{{ caption }}</span>

        {% set otherSendAppealsItems = [] %}
        {% for option in otherSendAppealsOptions %}
          {% set _ = otherSendAppealsItems.push({
            value: option.value,
            text: option.text,
            hint: { text: option.hint } if option.hint,
            checked: option.value == values.otherSendAppeals
          }) %}
        {% endfor %}
        {{ govukRadios({
          name: "otherSendAppeals",
          idPrefix: "otherSendAppeals",
          fieldset: { legend: { text: otherSendAppealsLabel, isPageHeading: true, classes: "govuk-fieldset__legend--l" } },
          items: otherSendAppealsItems,
          errorMessage: { text: errors.otherSendAppeals } if errors.otherSendAppeals
        }) }}

        {{ govukButton({ text: continueText or save }) }}
      </form>
    </div>
  </div>
{% endblock %}
