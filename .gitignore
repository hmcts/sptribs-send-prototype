node_modules/
dist/
build/
coverage/
.turbo/
.yarn/cache/
.yarn/install-state.gz
.yarn/build-state.yml
.yarn/unplugged/
.yarn/sdks/
.pnp.*
*.tsbuildinfo

# Test artefacts
playwright-report/
test-results/
allure-results/
allure-report/

# Editors
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.local
.env.*.local
config/local.json
config/local-*.json

# Helm rendered values
charts/**/values.aat.yaml
charts/**/values.preview.yaml

# Yarn audit scratch files, written into the workspace by the pipeline's
# yarn-audit-with-suppressions.sh. yarn-audit-known-issues itself IS committed.
active_suppressions
new_vulnerabilities
unneeded_suppressions
sorted-yarn-audit-*
yarn-audit-result*
yarn-audit-issues*
yarn-audit-known-issues-formatted
yarn-audit-known-issues-result
prettyPrintAudit.sh
transform-v4-to-v3-audit.cjs
